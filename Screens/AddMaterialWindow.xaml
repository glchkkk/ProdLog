<Window x:Class="ProdAndLog.Screens.AddMaterialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProdAndLog.Screens"
        mc:Ignorable="d"
        Title="Добавить материал" Height="350" Width="400">
    <Grid>
        <StackPanel Margin="15">
            <TextBlock Text="Код (уникальный):" Margin="0,0,0,2"/>
            <TextBox x:Name="txtCode" Margin="0,0,0,10"/>

            <TextBlock Text="Наименование:" Margin="0,0,0,2"/>
            <TextBox x:Name="txtName" Margin="0,0,0,10"/>

            <TextBlock Text="Тип материала:" Margin="0,0,0,2"/>
            <ComboBox x:Name="cmbType" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>

            <TextBlock Text="Единица измерения:" Margin="0,0,0,2"/>
            <TextBox x:Name="txtUnit" Text="кг" Margin="0,0,0,10"/>

            <TextBlock Text="Минимальный остаток:" Margin="0,0,0,2"/>
            <TextBox x:Name="txtMinStock" Text="0" Margin="0,0,0,10"/>

            <TextBlock Text="Рыночная цена (руб):" Margin="0,0,0,2"/>
            <TextBox x:Name="txtPrice" Text="0.00" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сохранить" Click="Save_Click" Width="90" Margin="0,0,10,0"/>
                <Button Content="Отмена" Click="Cancel_Click" Width="90"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
